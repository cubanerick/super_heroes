### Getting started
```
  In components/Theme.js

  const Theme = {
    links: [prod]
  }

  Change Theme.links to [dev] for localhost:3001


  npm install

  npm run dev

  npm run test
```

### Read `docs/*` for your task(s)